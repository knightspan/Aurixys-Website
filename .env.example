# Public site URL (used in metadata, sitemap, JSON-LD)
NEXT_PUBLIC_SITE_URL=https://aurixys.com

# Optional webhook the /api/contact route will forward submissions to.
# Leave empty in development — the route falls back to console.log.
NEXT_PUBLIC_CONTACT_WEBHOOK=
