# /assets/jal-prahari

Expected drops:

- `hero-render-001.png` — 3D render, 3/4 angle, transparent background, 2400px wide.
- `xray-render-001.png` — exploded / cutaway version of the same render.
- `jal-prahari.glb` — OPTIONAL. If present, the product page replaces the PNG with an interactive R3F viewer (OrbitControls, damping, no pan, mild auto-rotate).
- `jal-prahari-datasheet.pdf` — full technical spec PDF for the public download.

Pin coordinates for the x-ray callouts live in `lib/data/jal-prahari.ts` (`JP_XRAY_PINS`).
