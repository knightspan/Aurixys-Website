# /assets/hero

Expected drops:

- `jal-prahari-water.mp4` — 10–30s muted loop of the vessel on water. Desaturation applied via CSS.
- `jal-prahari-water-poster.jpg` — single frame from the video, 1920×1080. Used as the `<video poster>` and as the reduced-motion fallback.

If both files are absent, the Hero renders a styled placeholder background — the page is still shippable.
